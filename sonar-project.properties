sonar.projectKey=2401090_RealCode
sonar.projectName=2401090_RealCode
sonar.projectVersion=1.0

# Our code lives in both the Next.js client and Node server
sonar.sources=client/src,server

# We don't have a dedicated tests folder yet, but keep this for future use
sonar.tests=tests

# Common exclusions (node_modules, dist, tests, build outputs)
sonar.exclusions=**/node_modules/**,**/.next/**,**/dist/**,**/build/**,**/*.spec.js,**/*.test.js

# If you later add coverage, point this to your lcov file (e.g. from Jest)
sonar.javascript.lcov.reportPaths=coverage/lcov.info

sonar.sourceEncoding=UTF-8


